const gameTitleSchema = require('./gameTitle.schema');
const gameSchema = require('./game.schema');
const userSchema = require('./user.schema');

module.exports = { gameTitleSchema, gameSchema, userSchema };